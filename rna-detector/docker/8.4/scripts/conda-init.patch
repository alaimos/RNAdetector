
    if [ ! -d "/rnadetector/miniforge/bin" ]; then
        bash /rnadetector/scripts/install-snakemake.sh && source "${HOME}/.bashrc"
    else
        # >>> conda initialize >>>
        # !! Contents within this block are managed by 'conda init' !!
        __conda_setup="$('/rnadetector/miniforge/bin/conda' 'shell.bash' 'hook' 2> /dev/null)"
        if [ $? -eq 0 ]; then
            eval "$__conda_setup"
        else
            if [ -f "/rnadetector/miniforge/etc/profile.d/conda.sh" ]; then
                . "/rnadetector/miniforge/etc/profile.d/conda.sh"
            else
                export PATH="/rnadetector/miniforge/bin:$PATH"
            fi
        fi
        unset __conda_setup

        if [ -f "/rnadetector/miniforge/etc/profile.d/mamba.sh" ]; then
            . "/rnadetector/miniforge/etc/profile.d/mamba.sh"
        fi
        # <<< conda initialize <<<
    fi